import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FaceLab - Look, Feel, Live Better",
  description: "Advanced dental care by Dr. Wathsala & Dr. Manjula specializing in orthodontics, facial surgery, and 3D smile design.",
  keywords: "dental, orthodontics, facial surgery, 3D smile design, Sri Lanka, dentist",
  authors: [{ name: "FaceLab" }],
  creator: "FaceLab",
  publisher: "FaceLab",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
